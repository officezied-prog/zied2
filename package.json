{
  "name": "labsa",
  "version": "8.0.0",
  "private": true,
  "description": "Labsa — on-device virtual try-on. Single-file app plus its algorithm test suite.",
  "scripts": {
    "test": "node --test \"tests/*.test.mjs\"",
    "test:browser": "node tests/smoke.mjs"
  },
  "devDependencies": {
    "playwright": "1.49.1"
  }
}
